# Flask web framework
Flask==3.0.0
flask-cors==4.0.0

# PDF processing - use pymupdf which now includes pre-built wheels
# Note: Requires Python 3.8+ and provides wheels for Linux/Mac/Windows
pymupdf>=1.24.0

# HTTP requests for downloading FAA PDFs
requests==2.31.0

# Production WSGI server (required for Render)
gunicorn==21.2.0

# For scheduled tasks
APScheduler==3.10.4

# Environment variable management
python-dotenv==1.0.0
